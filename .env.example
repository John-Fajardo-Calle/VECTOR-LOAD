# Backend
ENGINE_URL=http://engine:6000
DATA_DIR=/app/data
ENGINE_TIMEOUT_S=300
ENABLE_TEST_ENDPOINT=0
# CORS_ORIGINS=https://your-frontend.example

# Backend bind (only used for local python runs; containers bind via Gunicorn)
HOST=0.0.0.0
PORT=5000

# Engine bind (only used if running engine service outside Docker)
ENGINE_HOST=0.0.0.0
ENGINE_PORT=6000
